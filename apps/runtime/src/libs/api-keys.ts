/**
 * @module
 * Compatibility re-export. The canonical implementation lives in
 * `@buntime/shared/api-keys` so plugins can depend on it too. Existing runtime
 * imports continue to work unchanged via this shim.
 */
export * from "@buntime/shared/api-keys";
